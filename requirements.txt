# Streamlit for building the web application
streamlit==1.57.0

# PyPDF2 for reading and extracting text from PDF files
PyPDF2==3.0.1

# python-dotenv for loading environment variables from a .env file
python-dotenv==1.0.0

# Langchain libraries for working with language models
langchain-core==1.4.0
langchain-community==0.4.2

# Requests for making HTTP requests to external APIs
requests==2.34.2

# urllib3 for handling URL requests
urllib3==2.0.7

# tqdm for progress bars
tqdm==4.66.1

# python-magic for file type detection
python-magic==0.4.27

# BeautifulSoup for parsing HTML and XML documents
beautifulsoup4==4.12.3

# NLTK for natural language processing tasks
nltk==3.8.1

# spaCy for lemmatization and phrase extraction
spacy==3.8.14

# sentence-transformers for semantic embeddings
sentence-transformers==2.7.0

# scikit-learn for TF-IDF and similarity scoring
scikit-learn==1.6.1

# Pydantic for data validation and settings management
pydantic==2.13.4

# typing-extensions for type hinting support
typing-extensions>=4.0.0

#Cross-platform lib for process and system monitoring in Python
psutil

# pytest for basic unit tests
pytest==8.2.0